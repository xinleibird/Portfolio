{
  "compilerOptions": {
    "allowJs": true,
    "checkJs": true,

    /* 1. 基础环境设置 */
    "target": "esnext", // 目标代码版本：使用最新的 JS 特性，不进行低版本转换
    "lib": [
      // 运行环境包含的库定义：
      "DOM", // 识别浏览器环境（window, document, <main>, <div> 等）
      "DOM.Iterable", // 允许对 NodeList 等进行循环操作
      "ESNext" // 支持最新的 JavaScript 内置对象
    ],

    /* 2. 模块处理 */
    "module": "esnext", // 模块规范：使用标准 import/export 语法
    "moduleResolution": "node", // 寻址策略：像 Node.js 一样从 node_modules 中找库
    // "esModuleInterop": true, // 兼容性：允许直接 import 那些没有默认导出的 CommonJS 库

    /* 3. React 特有配置 */
    "jsx": "react-jsx", // JSX 转换模式：

    // /* 4. 严格检查 */
    "strict": true, // 开启严格模式

    /* 5. 辅助提升 */
    "skipLibCheck": true, // 跳过库文件检查：不检查 node_modules 里的类型是否报错
    "isolatedModules": true, // 确保每个文件都能被独立编译（Vite 等打包工具的要求）
    "resolveJsonModule": true, // 允许直接 import .json 文件

    /* 6. 关闭 TS 检查 */
    "noImplicitAny": false, // 允许隐式的 any (这能解决大部分属性缺失报错)
    "strictNullChecks": false, // 关闭 null 检查 (解决很多 2322/2339)

    /* 7. 自定义路径 */
    "baseUrl": ".",
    "paths": {
      "/*": ["public/*"],
      "/images/*": ["public/images/*"],

      "#*": ["src/*"],
      "#assets/*": ["src/assets/*"],
      "#components/*": ["src/components/*"],
      "#constants/*": ["src/constants/*"]
    }
  },
  "include": ["src/**/*", "resolve.d.ts"],
  "exclude": ["node_modules", "dist"]
}
